
# Meant to be source'd. Set up environment variables for build and clean

export CC=clang
export CFLAGS="-O3"
export CFLAGS="-O0"
export CPPFLAGS="-Iintinclude -Iinclude"
export LD=clang++
export LDFLAGS="-Wl,-V -v"
